import { Routes } from '@angular/router';
import { ElementsPageComponent } from './pages/elements-page/elements-page.component';

export const routes: Routes = [{ path: '', component: ElementsPageComponent }];
